langcode: en
status: true
dependencies:
  enforced:
    module:
      - farm_crop_plan
      - farm_migrate
id: farm_migrate_plan_crop
label: 'Plans (Crop)'
migration_group: farm_migrate_plan
migration_tags:
  - 'Drupal 7'
  - 'farmOS 1.x'
class: Drupal\migrate\Plugin\Migration
field_plugin_method: null
cck_plugin_method: null
source:
  plugin: d7_plan
  bundle: crop
destination:
  plugin: 'entity:plan'
process:
  # Hard-code the bundle.
  type:
    plugin: default_value
    default_value: crop
  # Maintain the plan ID.
  id:
    plugin: get
    source: id
migration_dependencies:
  required: {  }
  optional: {  }
